spring.application.name=urban-settlement-mapping


# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload-dir=./uploads/issues/

# JWT Configuration (already implemented - placeholder)
jwt.secret=${JWT_SECRET:your-secret-key-change-in-production}
jwt.expiration=86400000

# Server Configuration
server.port=8080

# ===============================
# MongoDB Configuration
# ===============================
spring.data.mongodb.username=${MONGODB_USERNAME:tanaka}
spring.data.mongodb.password=${MONGODB_PASSWORD:admin@tanaka}
spring.data.mongodb.host=${MONGODB_HOST:localhost}
spring.data.mongodb.port=${MONGODB_PORT:27017}
spring.data.mongodb.database=${MONGODB_DATABASE:tanaka-db}
spring.data.mongodb.authentication-database=${MONGODB_AUTH_DB:admin}

# ===============================
# Mail Configuration
# ===============================
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:fadzainherera9@gmail.com}
spring.mail.password=${MAIL_PASSWORD:kqxljajvysyowxgh}
mail.properties.mail.smtp.auth=true
mail.properties.mail.smtp.starttls.enable=true

# ===============================
# Servlet Multipart Configuration
# ===============================
servlet.multipart.enabled=true
servlet.multipart.max-file-size=10MB
servlet.multipart.max-request-size=50MB
servlet.multipart.file-size-threshold=2KB
servlet.multipart.resolve-lazily=false

# ===============================
# Task Execution Configuration
# ===============================
task.execution.pool.core-size=2
task.execution.pool.max-size=4
task.execution.pool.queue-capacity=100
task.scheduling.pool.size=2

# ===============================
# Logging Configuration
# ===============================
logging.level.org.springframework.security=debug
logging.level.org.springframework.web=debug
logging.level.web=debug

# ===============================
# JWT Configuration
# ===============================
application.security.jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
application.security.jwt.expiration=86400000
application.security.jwt.refresh-token.expiration=604800000

# ===============================
# Python & ML Automation Configuration
# ===============================
ml.setup.enabled=true
ml.setup.scripts-dir=ml_scripts
ml.train.on-startup=false

# This path will be resolved once the MLInitializationService creates the venv
python.executable=${user.dir}/ml_scripts/venv/Scripts/python.exe
# For Linux/Mac use: python.executable=${PWD}/ml_scripts/venv/bin/python
